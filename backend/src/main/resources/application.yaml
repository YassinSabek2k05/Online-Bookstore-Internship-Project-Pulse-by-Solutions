spring:
  application:
    name: Online Bookstore Internship Project
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: 0000
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
jwt_cookie:
  value:
    staff_time: 86400000